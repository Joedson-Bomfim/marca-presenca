import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    botaoPrincipal: {
        padding: 4,
    },
    inputPadrao: {
        padding: 4,
    },
    buttonCadastraEdita: {
        marginBottom: 40
    }
})

export default styles;