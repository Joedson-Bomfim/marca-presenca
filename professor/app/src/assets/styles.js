import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    botaoPrincipal: {
        padding: 4,
    },
    inputPadrao: {
        padding: 4,
    },
    buttonCadastraEdita: {
        marginBottom: 40
    },
    botaoCadastro:{
       width: 200, 
       marginTop: 10,
       alignSelf: 'center',
    }
})

export default styles;