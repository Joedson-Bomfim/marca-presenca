import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    fundoTela: {
        flex: 1,
        padding: 20
    },
    marginBottom: {
        marginBottom: 30
    }
})

export default styles;